const UsedCards = ({ title, content }) => {
  return (
    <div className="single-used">
      <h1>{title}</h1>
      <p>{content}</p>
      <a href="">Get started &gt; &gt;</a>
    </div>
  );
};

export default UsedCards;
